name: Build JAR

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17
      - name: Build with Maven
        run: mvn package
      - uses: actions/upload-artifact@8e4d6e8e8fc55bdf50d7df5a11c2593a6d278b9e
        with:
          name: jar
          path: target/first-password-action.jar
